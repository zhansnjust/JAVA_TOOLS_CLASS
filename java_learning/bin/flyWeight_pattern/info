1.当多个对象相同或者相似时，可以使用享元模式节省内存.
2.所有的“池”都是享元模式， 线程池，数据连接池，常量池
3.优点：极大的减少了内存中的对象，相同或者相似的对象内存中只有一份，提高了系统的性能。
4、缺点：以时间换区空间， 而且程序逻辑复杂化

代码需求： 围棋只有黑白两个颜色，但是位置却有很多种， 我们提取相同的属性（颜色）作为内部属性，不同的属性(位置)作为外部属性。
在类中，把内部属性定义为成员变量， 外部属性通过类的形式传进来。 
提供一个工厂方法，map  ，键是内部属性，值是对象 